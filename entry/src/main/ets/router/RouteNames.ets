/** Stable route names used by the Navigation stack and route contracts. */
export enum RouteName {
  HOME = 'home',
  DIAGNOSIS = 'diagnosis',
  REPORT = 'report',
  TOOLS = 'tools',
  HISTORY_SETTINGS = 'historySettings',
  PRIVACY = 'privacy'
}
